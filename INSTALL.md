# התקנה - Karaoke

האפליקציה רצה על Windows 10/11 (64-bit). עדיף עם כרטיס מסך NVIDIA, אבל היא תעבוד גם בלי.

## איך זה עובד - שני שלבים

ההתקנה מתפצלת לשני קבצים:

1. **`KaraokeSetup-x.y.z.exe`** - קובץ קטן (כ-5-10 MB) שמוריד מ-GitHub Releases. זה ה-installer עצמו.
2. **קובץ ה-payload** (~3-4 GB) - הקבצים האמיתיים של האפליקציה. ה-installer מוריד אותם בזמן ההתקנה.

אתה לא צריך להוריד את ה-payload ידנית - ה-installer עושה את זה לבד. רק תוודא שיש לך אינטרנט תוך כדי ההתקנה.

## הורדה

הורד את `KaraokeSetup-x.y.z.exe` מ-GitHub Releases. זאת הורדה קטנה (שניות בודדות).

## התקנה - שלב אחר שלב

### 1. הרצת המתקין

לחץ פעמיים על הקובץ שהורדת.

### 2. אזהרת SmartScreen של Windows

תופיע חלונית כחולה עם הכותרת **"Windows protected your PC"** או **"Microsoft Defender SmartScreen prevented an unrecognized app from starting"**.

זה **לא** וירוס. זו אזהרה רגילה ש-Windows מציג לכל אפליקציה שלא חתומה דיגיטלית. ההסכמים של מיקרוסופט עולים כסף ולא טרחתי לחתום על הגרסה הזו.

איך לעקוף:

1. לחץ על **More info** (טקסט קטן באמצע החלונית)
2. תופיע שורה חדשה - לחץ על **Run anyway**

### 3. הגדרת מיקום ההתקנה

המתקין יציג מסך עם אפשרות לבחור איפה להתקין.

- **ברירת המחדל** היא `%LOCALAPPDATA%\Programs\Karaoke\` - תקין, אין צורך לשנות.
- ההתקנה היא **לא** דורשת הרשאות מנהל - אין צורך לאשר UAC.

### 4. הורדת ה-payload

לאחר שתלחץ **Install**, המתקין יראה בר התקדמות של "Downloading files..." או דומה. הוא מוריד את ה-payload (~3-4GB).

זה עשוי לקחת **5-20 דקות** תלוי באינטרנט שלך. ה-installer ישתמש בקובץ ה-payload לחילוץ הקבצים למיקום ההתקנה.

חשוב: אל תסגור את חלון ההתקנה ואל תנתק את האינטרנט באמצע - אחרת תצטרך להתחיל מחדש.

### 5. סיום

לחץ **Finish**. האפליקציה תיפתח אוטומטית.

## הפעלה ראשונה

ההפעלה הראשונה תעבוד כך:

1. **מסך פתיחה** - "ברוכים הבאים - קריוקי"
2. **זיהוי חומרה** - האפליקציה בודקת את הכרטיס מסך שלך
3. **הורדת מודל** (תלוי בחומרה):
   - **GTX 1060 6GB / RTX 2060 ומעלה (6+ GB VRAM)**: יורד מודל large-v3 (~2.9 GB). 3-7 דקות.
   - **GTX 1650 / 1660 (4-6 GB VRAM)**: יורד מודל medium (~1.5 GB). 2-4 דקות.
   - **כרטיס חלש יותר / ללא כרטיס**: משתמש במודל small המובנה, אין הורדה.

ההורדה רצה ברקע עם בר התקדמות. אם היא נכשלת באמצע, אפשר להמשיך אחר כך (היא יודעת לחזור על עצמה מהמקום שעצרה). בינתיים אפשר להמשיך עם המודל ה-small המובנה.

4. כשמסיימים, לוחצים **המשך** - מגיעים למסך הראשי.

## איך משתמשים

1. הדבק קישור YouTube של השיר
2. הדבק את המילים בעברית (אחת לשורה - אם השיר עם פזמון חוזר, רשום אותו פעמיים)
3. לחץ **צור קריוקי**
4. חכה 2-5 דקות בערך (תלוי באורך השיר ובחומרה)
5. הנגן ייפתח אוטומטית כשהכל מוכן

לחיצה על שבב הצבע למטה משנה את ערכת הצבעים של הנגן (יש 8 אפשרויות).

## פתרון בעיות

### "no GPU detected"

אם האפליקציה מציגה הודעה שלא נמצא כרטיס NVIDIA למרות שיש לך אחד:

1. ודא שמותקנים דרייברים עדכניים מאתר NVIDIA
2. הפעל מחדש את המחשב

### תקוע על "מוריד מ-YouTube"

הקישור ל-YouTube אולי לא תקין. נסה לפתוח אותו בדפדפן ולוודא שהשיר מתנגן. אם הסרטון מוגן זכויות יוצרים או חסום, ההורדה תיכשל.

### תקוע על "טוען מודל"

ההפעלה הראשונה אחרי התקנה חדשה אורכת 10-30 שניות. סבלנות.

### שגיאת CUDA / "GPU out of memory"

אם הכרטיס מסך שלך ממש קטן, נסה לסגור משחקים/דפדפנים פתוחים ולנסות שוב.

### האפליקציה לא נסגרת לגמרי

בדוק ב-Task Manager (Ctrl+Shift+Esc) אם יש תהליך `python.exe` שנשאר אחרי שסגרת. אם כן, סיים אותו ידנית. אם זה קורה לעיתים קרובות, דווח.

## הסרת התקנה

**Settings → Apps → Karaoke → Uninstall**.

חשוב לדעת:

- ספריית `%APPDATA%\Karaoke\` (שבה השירים שיצרת ומודלים שהורדת) **לא** נמחקת אוטומטית. אם אתה רוצה למחוק גם אותה - פתח את הסייר ב-`%APPDATA%\Karaoke` ומחק ידנית.
- אם תתקין שוב מאוחר יותר, השירים והמודלים יהיו עדיין שם.
