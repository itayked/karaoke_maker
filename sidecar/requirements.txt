# --- Core pipeline ---
yt-dlp>=2024.10.0
demucs>=4.0.1
stable-ts>=2.17.0          # stable-whisper (PyPI name)
torch>=2.2                  # install CUDA build separately first; see README
torchaudio>=2.2

# --- Audio utils ---
pyloudnorm>=0.1.1
soundfile>=0.12
numpy>=1.26,<2.0
silero-vad>=5.1

# --- Alignment helpers ---
python-Levenshtein>=0.25

# --- Sidecar HTTP layer (step 2) ---
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27                 # for tests of the HTTP layer

# --- Infra ---
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0

# --- Dev ---
pytest>=8.0
pytest-mock>=3.12
pytest-asyncio>=0.24
ruff>=0.5
